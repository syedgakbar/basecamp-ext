/*  Basecamp Extension, version 0.1.0 *  (c) 2010 Syed Ghulam akbar <akbar@jintech.com> * *   Chrome Content Page Library for the Chorme Extension. Performs the Content *   script specific actions *   http://code.google.com/chrome/extensions/ * *   Modification History /*--------------------------------------------------------------------------*/  // Handle the Chrome Extension (works only if running under google extension mode) var ChromeExtension = {	IsExtension : false,		ExtensionId : 'bkphfkjbnehgaiogmdfidipkahmjddbe',		// Returns true if this code is being run under the Google Chrome Extnesion	IsExtnesion : (function()	{		return (chrome && chrome.extension);	}),	// Initialize all the prototype functions	Initialize : (function()	{		ChromeExtension.IsExtension = (typeof(chrome) == "object" && chrome.extension);				// Check if running as Google Extension		if (!ChromeExtension.IsExtnesion) return;  	})}// Initialize the ExtensionChromeExtension.Initialize();